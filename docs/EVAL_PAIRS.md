# Eval corpus location

The runtime evaluation corpus ships as a package resource:

`src/nautical/resources/eval_pairs.json`

Do not maintain a second copy under docs/. Edit the packaged file and re-run `nautical eval`.

